<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.myapp.warest">

    <!-- To auto-complete the email text field in the login form with the user's emails -->
    <uses-permission android:name="android.permission.GET_ACCOUNTS" />
    <uses-permission android:name="android.permission.READ_PROFILE" />
    <uses-permission android:name="android.permission.READ_CONTACTS" />
	<uses-permission android:name="android.permission.INTERNET"/>

    <application
        android:allowBackup="true"
        android:icon="@drawable/warest"
        android:label="@string/app_name"
        android:roundIcon="@drawable/warest"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".StartScreenActivity"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
		
		<activity
            android:name=".ResetPasswordActivity"
            android:label="@string/app_name"
		></activity>

		<activity
            android:name=".SignupActivity"
            android:label="@string/app_name"
		/>

        <activity
            android:name=".MainActivity"
            android:label="@string/app_name"
            />
        <activity
            android:name=".LoginActivity"
            android:label="@string/app_name"
            />

        <activity
            android:name=".UpdateStudentProfile"
            android:label="@string/app_name"
            />

        <activity
            android:name=".StudentActivity"
            android:label="@string/app_name"
            />


        <activity
            android:name=".ViewStudentProfile"
            android:label="@string/app_name"
            />

        <activity
            android:name=".ContactusActivity"
            android:label="@string/app_name"
            />
        <activity
            android:name=".UploadTutorialActivity"
            android:label="@string/app_name"
            />

        <activity
            android:name=".UpdateTraineeProfile"
            android:label="@string/app_name"
            />


        <activity
            android:name=".AboutusActivity"
            android:label="@string/app_name"
            />

        <activity
            android:name=".FacultyActivity"
            android:label="@string/app_name"
            />
        <activity
            android:name=".TutorialNotesActivity"
            android:label="@string/app_name"
            />

        <activity
            android:name=".SelectFacultyType"
            android:label="@string/app_name"
            />

        <activity
                    android:name=".AdminActivity"
                  android:label="@string/app_name"
                    />



        <activity
            android:name=".WorkshopActivity"
            android:label="@string/app_name"
            />

        <activity
            android:name=".WorkshopViewActivity"
            android:label="@string/app_name"
            />


        <activity
            android:name=".UpdateFreelancerProfile"
            android:label="@string/app_name"
            />


        <activity
            android:name=".OrganisationActivity"
            android:label="@string/app_name"
            />


        <activity
            android:name=".SelectOrganisation"
            android:label="@string/app_name"
            />

        <activity
            android:name=".CollegeProfile"
            android:label="@string/app_name"
            />

        <activity
            android:name=".CorporateProfile"
            android:label="@string/app_name"
            />

        <activity
            android:name=".TrainingProfile"
            android:label="@string/app_name"
            />











    </application>

</manifest>
